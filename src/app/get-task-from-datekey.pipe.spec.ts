import { GetTaskFromDatekeyPipe } from './get-task-from-datekey.pipe';

describe('GetTaskFromDatekeyPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTaskFromDatekeyPipe();
    expect(pipe).toBeTruthy();
  });
});
